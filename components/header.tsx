import { Button } from "./ui/button";

const Header = () => {
  return (
    <header>
      <Button>Connexion</Button>
      <Button>Deconnexion</Button>
    </header>
  );
};

export default Header;
